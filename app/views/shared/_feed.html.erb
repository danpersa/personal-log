<% unless @feed_items.empty? %>
  <table class="ideas" summary="User ideas">
    <%= render :partial => 'shared/feed_item_idea', :collection => @feed_items %>
  </table>
  <%= will_paginate @feed_items %>
<% end %>
